import Layout from '../components/Layout'
import AboutSection from '../components/sections/About'

// /about — the studio and the model.
export default function About() {
  return (
    <Layout>
      <AboutSection />
    </Layout>
  )
}
